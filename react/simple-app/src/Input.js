import React, {PropTypes} from 'react'

// your code here! either a function or a class will work. starter for a function is below.

const Input = props => // ??

Input.propTypes = { changename : PropTypes.func }
